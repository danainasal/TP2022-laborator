int Fact(int n)
{	if (n == 0)
return 1;
else
return n * Fact(n - 1);
}

int main()
{
	int nr;
	printf("nr= ");
	scanf("%d", &nr);
	printf("Factorialul  este : %d\n", Fact(nr));
	system("pause");
	return 0;
}
